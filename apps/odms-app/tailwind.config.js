/** @type {import('tailwindcss').Config} */

/** @note we're importing our shared tailwind config from the dedicated `odms-ui` package  */
import * as config from "odms-ui/tailwind.config";

module.exports = config;
