import * as React from "react";

/** @note {{note}} */

interface {{ pascalCase name }}Props {
  children?: React.ReactNode;
}

const {{ pascalCase name }} = ({ children }: {{ pascalCase name }}Props):React.ReactNode => {
  return (
    <div>
      <h1>{{ name }}</h1>
      {children}
    </div>
  );
};

export default {{ pascalCase name }}